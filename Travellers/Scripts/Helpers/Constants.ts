class Constants {
    // -----------------------
    // Constants
    // -----------------------

    static SCREEN_WIDTH: number = 960;
    static SCREEN_HEIGHT: number = 640;

    static CELL_SIZE = 64;
    static CELLS_HORIZONTAL = 13;
    static CELLS_VERTICAL = 9;

    static FIELD_OFFSET: number = 32;
    static FIELD_WIDTH: number = Constants.CELL_SIZE * Constants.CELLS_HORIZONTAL;
    static FIELD_HEIGHT: number = Constants.CELL_SIZE * Constants.CELLS_VERTICAL;
} 