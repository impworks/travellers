class Constants {
    // -----------------------
    // Constants
    // -----------------------

    static SCREEN_WIDTH = 960;
    static SCREEN_HEIGHT = 640;

    static CELL_SIZE = 96;
    static CELLS_HORIZONTAL = 8;
    static CELLS_VERTICAL = 6;

    static FIELD_OFFSET = 32;
    static FIELD_WIDTH = Constants.CELL_SIZE * Constants.CELLS_HORIZONTAL;
    static FIELD_HEIGHT = Constants.CELL_SIZE * Constants.CELLS_VERTICAL;

    static DEFAULT_SPEED = 8;
} 